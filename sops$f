---

## On Error

For all connection, MCP, CDP, Gemini, and Redis failures see `sops/sop_on_error_shared.md`.

**This SOP specific failures:**
---

## On Error

For all connection, MCP, CDP, Gemini, and Redis failures see `sops/sop_on_error_shared.md`.

**This SOP specific failures:**
---

## On Error

For all connection, MCP, CDP, Gemini, and Redis failures see `sops/sop_on_error_shared.md`.

**This SOP specific failures:**
---

## On Error

For all connection, MCP, CDP, Gemini, and Redis failures see `sops/sop_on_error_shared.md`.

**This SOP specific failures:**
---

## On Error

For all connection, MCP, CDP, Gemini, and Redis failures see `sops/sop_on_error_shared.md`.

**This SOP specific failures:**
---

## On Error

For all connection, MCP, CDP, Gemini, and Redis failures see `sops/sop_on_error_shared.md`.

**This SOP specific failures:**
